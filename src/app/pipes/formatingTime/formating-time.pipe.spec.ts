import { FormatingTimePipe } from './formating-time.pipe';

describe('FormatingTimePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatingTimePipe();
    expect(pipe).toBeTruthy();
  });
});
