import { ReduceReleaseDatePipe } from './reduce-release-date.pipe';

describe('ReduceReleaseDatePipe', () => {
  it('create an instance', () => {
    const pipe = new ReduceReleaseDatePipe();
    expect(pipe).toBeTruthy();
  });
});
