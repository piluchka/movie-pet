import { MathRoundPipe } from './math-round.pipe';

describe('MathRoundPipe', () => {
  it('create an instance', () => {
    const pipe = new MathRoundPipe();
    expect(pipe).toBeTruthy();
  });
});
